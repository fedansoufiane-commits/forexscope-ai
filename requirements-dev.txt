# Only needed to regenerate the deliverable documents - the app, the notebooks
# and the test suite run without these.
#
#   pip install -r requirements-dev.txt
#   python3 scripts/build_wealthscope_report.py   # five-page Ausarbeitung (.docx)
#   python3 scripts/refresh_poster.py             # A3 poster figures and metrics
#
python-docx>=1.1,<2
python-pptx>=0.6.23,<2
kaleido>=0.2,<2
